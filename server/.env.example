# BigQuery設定（オプション）
# これらの環境変数を設定することで、BigQueryからデータを取得できます
# 設定しない場合は、モックデータが使用されます

# Google Cloud Project ID
GOOGLE_CLOUD_PROJECT_ID=your-project-id

# BigQuery Dataset名
BIGQUERY_DATASET=your_dataset_name

# BigQuery Table名
BIGQUERY_TABLE=your_table_name

# BigQuery Location（オプション、デフォルト: US）
BIGQUERY_LOCATION=US

# Google Application Credentials（サービスアカウントキーファイルのパス）
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# カスタムクエリ（オプション）
# BIGQUERY_BUILDINGS_QUERY=SELECT * FROM `your_project.your_dataset.your_table` WHERE active = true
# BIGQUERY_BUILDING_DETAIL_QUERY=SELECT * FROM `your_project.your_dataset.your_table` WHERE id = @id

# サーバー設定
NODE_ENV=development
PORT=8080
